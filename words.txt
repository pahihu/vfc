( run-time)
        <LIT> 0BRANCH BRANCH <NEXT> <C">

( comparison)
        < = > 0= NOT

( arithmetic and logical)
        1+ 1- 2* 2/
        CELL CELLS CELL+
        + - * / MOD /MOD */ ABS NEGATE MIN MAX
        AND OR XOR INVERT

( control structures)
        IF ELSE THEN 
        FOR I NEXT
        BEGIN WHILE REPEAT UNTIL
        EXIT EXECUTE

( numeric conversion)
        BASE DECIMAL HEX . .R

( defining)
        : ; VARIABLE CONSTANT CREATE DOES>

( compiler)
        , ALLOT C" ]

( miscellaneous)
        ( HERE PAD ABORT COLD ?MS


~~~
( stack)
        DROP DUP SWAP OVER >R R@ R>

( memory)
        @ ! +! C@ C! MOVE FILL

( terminal)
        CR SPACE SPACES EMIT KEY WORD

( strings)
        COUNT PLACE APPEND TYPE -TRAILING

( storage)
        BLOCK SAVE LOAD OFFSET
        INCLUDE

( foreign-function interface)
        (DLOPEN) (DLSYM) (CALLC) SP@ ZCOUNT

( dictionary)
        MACRO FORTH MARK EMPTY '

